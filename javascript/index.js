$(document).ready(function () {
   $('document').ready(function () {
      var options = { videoId: 'FBtWixtj4NQ', mute: true };
      $('header, #main_visual, section, #content, .con, footer, .in3').tubular(options);
   });
});
